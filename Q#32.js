let current_users = ['eric', 'willie', 'admin', 'erin', 'Ever']
let new_users = ['sarah', 'Willie', 'PHIL', 'ever', 'Iona']

let current_users_lower = user.toLowerCase() 
for (user=0; user <current_users.length; user++){

for (new_user=0; new_user < new_users.length;new_user++){
    if (new_user.toLowerCase() = current_users_lower){
        print("Sorry " + new_user + ", that name is taken.")
    }
    else{
        print("Great, " + new_user + " is still available.")
    }
}}